---
description: Admin Rules
globs: ['admin/**']
alwaysApply: true
---

- The project is already running on port :6053. Do NOT try to run the project.
- The API docs can be accessed using this url: http://localhost:6052/api-docs/swagger.json. Always use curl command from this device to fetch the docs instead of doing a "searching web". You can also check backend/src/routes to see the available apis.